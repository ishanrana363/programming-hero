const man = {
    name : "kodu",
    height : 6,
    age : 32,
    dis : "dinajpur",
    isHonest : true
};

console.log(typeof man)

const parseValuetoJson = JSON.stringify(man);

console.log( typeof parseValuetoJson);