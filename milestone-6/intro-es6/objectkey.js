const person = {
    name : "ishan",
    roll : 500510,
    height : "6 inch",
    age : 21
};

const keysValue = Object.keys(person);
const objectValues = Object.values(person)

console.log(keysValue);

console.log(objectValues);